# Runtime deps for the ChronoLens Lambda. SAM's Python builder installs these
# into the deployment package during `sam build`. boto3 is already provided by
# the Lambda runtime, so it's intentionally omitted.
httpx>=0.27
python-dotenv>=1.0
opentelemetry-sdk>=1.25
opentelemetry-exporter-otlp-proto-grpc>=1.25
